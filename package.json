{
  "name": "abuseflow",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "build:watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "test:provider-dom": "node scripts/run-provider-dom-fixtures.mjs",
    "test:fixtures": "node scripts/run-fixture-tests.mjs",
    "test": "npm run test:fixtures && npm run test:provider-dom"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.296",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "esbuild": "^0.25.0",
    "typescript": "^5.6.3"
  }
}
